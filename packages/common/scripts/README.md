# @lms/common-scripts

This library was generated with [@aws/nx-plugin](https://github.com/awslabs/nx-plugin-for-aws/).

## Building

Run `pnpm nx build @lms/common-scripts [--skip-nx-cache]` to build the application.

## Running unit tests

Run `pnpm nx test @lms/common-scripts` to execute the unit tests via Vitest.

### Updating snapshots

To update snapshots, run the following command:

`pnpm nx test @lms/common-scripts --configuration=update-snapshot`

## Run lint

Run `pnpm nx lint @lms/common-scripts`

### Fixable issues

You can also automatiaclly fix some lint errors by running the following command:

`pnpm nx lint @lms/common-scripts --configuration=fix`

## Useful links

- [common-scripts reference docs](TODO)
- [Learn more about NX](https://nx.dev/getting-started/intro)

